#include<bits/stdc++.h>
using namespace std;

void printReverse(string s, int ind){
    if(s.size() == ind)return;
    printReverse(string)
}

int main(){
    string s;
    cin >> s;
    printReverse(s, 1);
    return 0;
}
