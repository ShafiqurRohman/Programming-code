#include<stdio.h>
int main()
{
    int x,y,sum;
    printf("Enter the first value:");
    scanf("%d",&x);
    printf("Enter the second value:");
    scanf("%d",&y);

    sum=x+y;
    printf("the sum is:%d",sum);
}
