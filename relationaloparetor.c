#include<stdio.h>
#include<math.h>
 int main()
{
    int num;
    printf("enter an integer:");
    scanf("%d",&num);
    if(num%2==0)
        printf("the integer is even");
    else
        printf("the integer is 0dd");
    return 0;
}
