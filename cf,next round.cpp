#include<bits/stdc++.h>
using namespace std;
int main(){
    int a,b,c=0,n;
    vector<int>v;
    cin>>a>>b;
    while(a--){
        cin>>n;
       v. push_back(n);
       }
       for(int i=0;i<v.size();i++){

        if(v[i]>=v[b-1] && v[i]>0)c++;
    }
    cout<<c<<endl;
return 0;
}
